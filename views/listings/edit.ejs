<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="row mt-3">
        <div class="col-8 offset-2">
            <h3>Edit your Listing</h3>
            <form action="/listings/<%= listing.id  %>?_method=put" method="post" novalidate class="needs-validation">
        <div>
            <label for="title" class="form-label">Title</label>
            <input type="text" name="listing[title]" id="title" value="<%= listing.title %>" class="form-control" required>
            <div class="invalid-feedback">Enter Valid Title</div>
            <br><br>
        </div>
        <div>
            <label for="image" class="form-label">Image link</label>
        <input type="text" name="listing[image]" id="img" value="<%= listing.image %>" class="form-control" required>
        <div class="invalid-feedback">Enter Valid Image(url)</div>
        <br><br>
        </div>
        <div>
            <label for="description" class="form-label">Description</label>
            <textarea name="listing[description]" id="desp"  class="form-control" required><%= listing.description %> </textarea>
            <div class="invalid-feedback">Enter Valid Description</div>
            <br><br>
        </div>   
        <div>
            <label for="price" class="form-label">Price</label>
            <input type="number" name="listing[price]" id="prc" class="form-control" value="<%= listing.price %>"  required>
            <div class="invalid-feedback">Enter Valid Price</div>
            <br><br>
        </div>
        <div class="row">
            <div class="col-8">
                <label for="location" class="form-label">Location</label>
                <input type="text" name="listing[location]" id="loc" class="form-control" value="<%= listing.location %>" required>
                <div class="invalid-feedback">Enter Valid Location</div>
                <br><br>
            </div>
            <div class="col-4">
                <label for="country" class="form-label">Country</label>
                <input type="text" name="listing[country]" id="cntry" class="form-control" value="<%= listing.country %>" required>
                <div class="invalid-feedback">Enter Valid Country</div>
            </div>
        </div>
        <div class="mb-3">
        <button class="btn btn-dark edit-btn mt-2">Edit</button>
        </div>
        
    </form>
        </div>
    </div>    
</body>
</html>